(ns shopping-app-web.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [shopping-app-web.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
